name: Build TapForge 2.1 Pulse

on:
  push:
    branches:
      - pulse-2.1-build

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore TapForge 2.1 source
        run: |
          mkdir -p /tmp/tapforge-2.1
          base64 -d .tapforge-build/pulse-2.1.b64 > /tmp/tapforge-2.1.tar.xz
          tar -xJf /tmp/tapforge-2.1.tar.xz -C /tmp/tapforge-2.1

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install SDK components
        run: sdkmanager "platforms;android-35" "build-tools;34.0.0" "platform-tools"

      - name: Install Gradle 8.9
        run: |
          curl -fsSL https://services.gradle.org/distributions/gradle-8.9-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /tmp

      - name: Build TapForge 2.1
        working-directory: /tmp/tapforge-2.1
        run: /tmp/gradle-8.9/bin/gradle --no-daemon clean assembleDebug assembleRelease

      - name: Collect APKs
        run: |
          cp /tmp/tapforge-2.1/app/build/outputs/apk/debug/app-debug.apk /tmp/TapForge-2.1-Pulse-test.apk
          cp /tmp/tapforge-2.1/app/build/outputs/apk/release/app-release-unsigned.apk /tmp/TapForge-2.1-Pulse-release-unsigned.apk

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: TapForge-2.1-Pulse-build
          path: |
            /tmp/TapForge-2.1-Pulse-test.apk
            /tmp/TapForge-2.1-Pulse-release-unsigned.apk
          if-no-files-found: error
